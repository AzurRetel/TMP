
  if [log][file][path] {
    mutate {
      add_field => { "ORIGEN_ARCHIVO" => "%{[log][file][path]}" }
    }
  }

  # 2. Borramos los objetos pesados de Filebeat y el duplicado "event" (donde venía event.original)
  mutate {
    remove_field => [ "agent", "host", "ecs", "input", "log", "event", "@version" ]
  }